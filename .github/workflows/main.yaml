name: pull-request
on:
  pull_request:
    branches: [ main ]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10.4'
      - name: Install pipenv
        run: python -m pip install pipenv
      - name: Install dependencies
        run: python -m pipenv install --dev
      - name: Run tests
        run: pipenv run black --diff --check dj_yosof/
